AWSTemplateFormatVersion: '2010-09-09'

Description: Simple EC2 Deployment using CloudFormation

Resources:

  RamaSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow SSH and HTTP

      SecurityGroupIngress:

        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0

        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0

  SurakshithaEC2Instance:
    Type: AWS::EC2::Instance

    Properties:
      ImageId: ami-00e801948462f718a
      InstanceType: t2.micro
      KeyName: surakshitha-keypair

      SecurityGroups:
        - !Ref RamaSecurityGroup

      Tags:
        - Key: Name
          Value: SurakshithaEC2Instance-Day09